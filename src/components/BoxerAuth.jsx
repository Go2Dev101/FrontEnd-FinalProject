export const BoxerAuth = ({children}) => {
  return (
    <div className="min-h-[calc(100vh-172px)] flex flex-col justify-center items-center bg-background-100 px-3 py-6">
        {children}
    </div>
  )
}
