import { Menu } from "../../components/Menu"
import { MenuCard } from "../../components/MenuCard"

export const MenuSet = () => {
  return (
    <div>
      <Menu>
        <MenuCard/>
      </Menu>
    </div>
  )
}
